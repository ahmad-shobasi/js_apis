version: '3.9'

services:
  redis:
    image: redis:7
    container_name: nest_redis
    ports:
      - '6379:6379'
    restart: always

  redis-insight:
    image: redis/redisinsight:latest
    container_name: redis_gui
    ports:
      - '8001:5540'
