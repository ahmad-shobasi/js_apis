NODE_ENV=development
PORT=
APP_URL=
DATABASE_URL=
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d
REDIS_HOST=
REDIS_PORT=

# Real SMTP (required for delivery to real inboxes — not Ethereal test mail)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
# Optional; defaults to SMTP_USER if unset. Use a verified sender for your provider.
MAIL_FROM=